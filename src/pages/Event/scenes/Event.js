import React from 'react'

const Event = (props) => {
    console.log(props)
    // const {name, eventStart, eventEnd, budget, description, location} = props.event

    return (
        <div>
            {/* <span>{name}</span>
            <p>{eventStart} to {eventEnd}</p>
            <p>Budget: ${budget}</p> */}
        </div>
    )
}

export default Event
